- In the location we should show the dropdown from which we can select location
- in budget numerical value we can in put rather than low mediium high
(change the logic in all  prompt as well)
shortlist size ( max rows sent to the model) is not required
we can fix amount of restraunt we are sending to the model